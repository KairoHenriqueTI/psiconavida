# Copy to .env and fill values
DATABASE_URL=postgresql://USER:PASS@HOST:5432/DATABASE
NEXTAUTH_URL=https://api.psico.kairohenrique.dev
NEXTAUTH_SECRET=replace-with-a-secure-random-string
FRONTEND_URL=https://psico.kairohenrique.dev
POSTGRES_USER=pnv_user
POSTGRES_PASSWORD=pnv_pass
POSTGRES_DB=pnv_db

# SMTP for NextAuth Email (Magic Link)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="noreply@example.com"

# OpenAI (server-side)
OPENAI_API_KEY=

# Optional: PORT
PORT=3001
